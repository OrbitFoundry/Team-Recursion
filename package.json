{
  "name": "cooked-placement-portal",
  "version": "1.0.0",
  "description": "Root workspace delegator for cooked? placement portal",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
